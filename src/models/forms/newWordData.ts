type NewWordData = {
  swearing: boolean;
  usages: string;
  tags: string;
  helperText?: string;
  wordname: string;
  meaning: string;
  extended_description: string;
};

export default NewWordData;
