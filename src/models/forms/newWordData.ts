type NewWordData = {
  wordname: string;
  meaning: string;
  swearing: boolean;
  extended_description: string;
  usages: string;
  tags: string;
  helperText?: string;
};

export default NewWordData;
